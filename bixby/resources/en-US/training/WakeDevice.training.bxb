training {
  // Pattern: explicit device name
  "[wake|turn on|start|boot] [my|the] #{value(WakeDevice.deviceName)} [in Aweken|using Aweken|with Aweken]" {
    goal: WakeDevice
    value-set {
      WakeDevice.deviceName = #{value(WakeDevice.deviceName)}
    }
  }

  // Pattern: "wake my PC" without a specific name — Bixby will ask which device
  "[wake|turn on|start|boot] [my|the] [PC|computer|laptop|device|machine] [in Aweken|using Aweken|with Aweken]" {
    goal: WakeDevice
  }

  // Minimal pattern
  "[wake|power on] #{value(WakeDevice.deviceName)}" {
    goal: WakeDevice
    value-set {
      WakeDevice.deviceName = #{value(WakeDevice.deviceName)}
    }
  }
}
